name: server-control-test-pulumi
description: Server Control Test Env Setup
runtime: nodejs
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-west-2

